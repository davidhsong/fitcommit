"use client";

import {
  LineChart,
  Line,
  XAxis,
  YAxis,
  Tooltip,
  ResponsiveContainer,
  CartesianGrid,
} from "recharts";
import { Scale, Activity, Trophy, Calendar, ArrowUpRight } from "lucide-react";

type Log = {
  exercise?: string;
  weight?: string;
  sets?: string;
  isPR?: boolean;
};

export default function dashboard() {}
